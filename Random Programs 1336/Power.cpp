#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;
int main() {

    int x, y;
    cout << "Enter the values of x and y: ";
    cin >> x >> y;

    cout << "The value of " << x << " to the power of " << y << " is " << pow(x,y) << endl;


   return 0;
}
