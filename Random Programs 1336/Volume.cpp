#include <iostream>
using namespace std;
int main() {

    int length, width, height, volume;
    cout << "Enter the length: ";
    cin >> length;

    cout << "Enter the width: ";
    cin >> width;

    cout << "Enter the height: ";
    cin >> height;

    volume = length * width * height;

    cout << "The volume of a cuboid is: " << volume << endyes

  return 0;
}
